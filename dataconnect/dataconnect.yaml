specVersion: "v1"
serviceId: "hipaa-backend"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "moonraker-engage"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
